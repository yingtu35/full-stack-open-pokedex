name: Hello World

on:
  push:
    branches:
      - master

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello World!"
      - name: Date
        run: date
      - name: List Directory
        run: |
          ls -l
